@@ -32,6 +32,7 @@
 import net.minecraft.core.RegistryAccess;
 import net.minecraft.network.chat.CommonComponents;
 import net.minecraft.network.chat.Component;
+import net.minecraft.network.chat.TextComponent;
 import net.minecraft.network.chat.TranslatableComponent;
 import net.minecraft.resources.ResourceLocation;
 import net.minecraft.server.MinecraftServer;
@@ -43,6 +44,7 @@
 import net.minecraftforge.api.distmarker.OnlyIn;
 import org.apache.logging.log4j.LogManager;
 import org.apache.logging.log4j.Logger;
+import si.f5.pa_union.update.UpdateScreen;
 
 @OnlyIn(Dist.CLIENT)
 public class TitleScreen extends Screen {
@@ -159,8 +161,11 @@
          Screen screen = (Screen)(this.minecraft.options.skipMultiplayerWarning ? new JoinMultiplayerScreen(this) : new SafetyScreen(this));
          this.minecraft.setScreen(screen);
       }, button$ontooltip))).active = flag;
-      (this.addRenderableWidget(new Button(this.width / 2 - 100, p_96764_ + p_96765_ * 2, 200, 20, new TranslatableComponent("menu.online"), (p_96776_) -> {
+      (this.addRenderableWidget(new Button(this.width / 2 + 2, p_96764_ + p_96765_ * 2, 98, 20, new TranslatableComponent("menu.online"), (p_96786_) -> {
          this.realmsButtonClicked();
+      }))).active = flag;
+      (this.addRenderableWidget(new Button(this.width / 2 - 100, p_96764_ + p_96765_ * 2, 98, 20, new TextComponent("更新情報"), (p_96788_) -> {
+         this.minecraft.setScreen(new UpdateScreen());
       }, button$ontooltip))).active = flag;
    }
 
g;
    }
 
    private void createDemoMenuOptions(int p_96773_, int p_96774_) {
